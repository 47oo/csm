# CSM 项目全局规则

## 1. 项目定位

CSM 是面向 HPC / AI 运维场景的内部资源管理平台。

平台主要目标是替代分散维护的 Excel 表格，为资源提供统一的登记、查询、维护、关联、状态查看和资源使用情况查看。

CSM 不以建设完整 CMDB 为目标。

具体产品范围和业务规则以 `docs/product/` 中已经确认的文档为准。

---

## 2. 开发基本原则

1. 在开始实现之前，先理解需求及预期行为。
2. 不得自行创造尚未确认的业务规则。
3. 优先采用简单、清晰、易维护的实现，不进行没有实际需求支撑的过度抽象。
4. 业务规则必须显式表达，避免隐藏在代码实现细节中。
5. 产品需求、领域规则、架构决策和具体实现应尽量分离。
6. 没有明确收益时，不引入额外框架、基础设施或架构复杂度。
7. 已确认的业务规则不得在开发过程中被静默修改。
8. 当歧义会影响业务行为或架构设计时，必须明确指出，不得自行做永久性决定。
9. 不得默认技术栈、部署架构、详细数据模型或 API 规范已经确定；以已确认文档和已批准架构决策为准。

---

## 3. 领域模型与权威来源

CSM 的权威领域模型位于：

`docs/product/domain-model.md`

涉及资源类型、资源分类、资源关系、状态模型、唯一性规则和生命周期时，必须优先读取该文档。

`.pi/skills/resource-domain/SKILL.md` 提供面向 Agent 的精简领域知识和分析规则。

不得在各 Agent 定义中复制完整领域模型。

当信息发生冲突时，按以下优先级判断：

1. 用户最新明确确认的需求；
2. `docs/product/domain-model.md` 与 `docs/product/` 中已经确认的产品文档；
3. `docs/architecture/` 中已经批准的架构决策；
4. 项目 Skill 中已经确认的领域规则；
5. 当前代码实现；
6. 通用工程经验和默认做法。

低优先级信息不得静默覆盖高优先级信息。

如果当前代码实现与已确认需求冲突，默认以需求为准，除非用户明确要求修改需求。

---

## 4. 文档与目录规则

重要的产品规则和架构决策不能只存在于聊天记录或源代码中。

* 产品需求与领域规则写入 `docs/product/`。
* 架构决策写入 `docs/architecture/`。
* 数据库设计与 Migration 说明写入 `docs/database/`。
* API 契约写入 `docs/api/`。
* 项目计划与当前执行状态以 `docs/project/project-plan.yaml` 为准。

新增或修改长期有效规则时，必须同步更新对应文档。

同一份详细信息只维护一个权威来源，其他位置通过引用使用。

---

## 5. 变更纪律

实现非简单功能前，必须：

1. 理解需求及预期行为；
2. 找出涉及的领域规则；
3. 检查已有实现；
4. 判断是否涉及架构或数据库 Schema 变更；
5. 确定验证方法。

代码变更应保持聚焦，不得顺便进行无关的大范围重构，除非是保证正确性所必需，或用户明确要求。

---

## 6. 数据库安全

数据库变更属于长期兼容性变更，必须考虑已有数据与 Migration 策略。

不得：

* 未经确认删除重要资源历史；
* 未考虑已有数据就执行破坏性 Schema 修改；
* 对能够由数据库可靠保证的重要完整性约束，仅依赖应用代码实现。

重要业务约束必须有明确说明和对应测试。

---

## 7. 测试与 Review

每个实现任务都必须明确验证方法，并实际执行适用的验证。

修复 Bug 时，在条件允许的情况下增加回归测试。

未实际验证的内容必须明确说明，不得声称已经正确工作。

在条件允许的情况下，实现和 Review 应由不同角色负责。

Review 必须检查：需求与领域规则符合性、架构符合性、功能正确性、可维护性和测试充分性。

不能仅因代码“看起来合理”就批准。

---

## 8. Agent 协作

各 Agent 必须遵守 `.pi/agents/` 中定义的角色职责和边界，不得越权代替其他角色做业务或技术决策。

交接必须保留：已确认需求、已做决策、关键约束、当前假设、未解决问题和后续关注事项。

* Agent 定义角色职责；
* Skill 提供能力、精简领域知识和分析方法；
* Docs 保存权威事实；
* Prompt 定义具体工作流。

---

## 9. Git 安全与流程入口

所有专业 Subagent 只负责职责内修改、测试与报告。Git 状态由主协调 Agent 统一管理：并行任务必须划定文件所有权；所有写入任务停止后才能暂存、提交或切分支。

### 9.1 Subagent 的 Git 声明（交接的验收条件）

每份 Subagent 报告必须以下列之一结尾：

```text
GIT: NONE
```

或逐条列出本次实际执行的 Git 命令（不概括、不省略）：

```text
GIT: git rev-parse --short HEAD
GIT: git diff --stat
```

协调器接收交接前，优先对照 Subagent 的工具调用记录核对 Git 命令及声明；`git reflog` 和当前 Git 状态只用于辅助核对引用变更，**不能用 reflog 证明只读命令已执行或未执行**。声明缺失、与可见工具记录不符或漏列已记录命令时，退回该 Agent 重新报告；若工具记录不可用，明确标记该项无法核实，不得宣称已用 reflog 完成逐条核对。发现 Subagent 改变 Git 状态时停止交接并核对现场。

### 9.2 禁止事项

Subagent 不得执行 `git add`、Commit、Branch 切换、Merge，以及其他改变 Git 状态的操作。

不得自动提交未知改动，不得使用自动 stash、`reset --hard`、`clean`、强制切分支或改写历史来绕过工作区问题。

### 9.3 流程入口

执行 Git 操作和判断 Feature 完成状态前，必须读取并遵守：

* `docs/project/git-workflow.md`：分支、提交、Review 范围、Merge Gate 与 DONE 标准；
* `.pi/prompts/implement-project.md`：Git 元数据、阶段执行与恢复调度；
* `.pi/prompts/project.md`：生成或更新项目计划时的规则。

项目状态必须有真实的提交、测试和 Review 证据支持；Review 批准不等于 Feature DONE。
