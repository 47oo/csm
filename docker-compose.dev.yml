# ============================================================================
# CSM 本地开发用 PostgreSQL（仅 dev / test）
#
# ⚠️ 这是 dev / test 专用的本地数据库，不是生产交付物。
#    生产内网部署见 docker-compose.prod.yml 与
#    docs/deployment/csm-v1-internal-deployment.md。
#    本文件只提供「30 分钟内可启动」所需的本地数据库。
#
# 关键点：显式固定 locale / encoding，避免镜像默认值漂移导致「大小写敏感」
# 与中文排序语义被静默改变（ADR-0002 裁定 1 / 架构 Risk #1）。
# ============================================================================
name: csm-dev

services:
  postgres:
    image: postgres:16
    container_name: csm-dev-postgres
    environment:
      POSTGRES_USER: csm
      POSTGRES_PASSWORD: csm
      POSTGRES_DB: csm
      # UTF8 + 大小写敏感的 locale（C.UTF-8）。
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C.UTF-8"
      LANG: C.UTF-8
      LC_ALL: C.UTF-8
    ports:
      - "5432:5432"
    volumes:
      - csm-dev-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U csm -d csm"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  csm-dev-pgdata: