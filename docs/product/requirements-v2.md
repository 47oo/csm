# CSM V2 产品需求

> Status: **DRAFT — AWAITING CONTENT**（等待用户提供 v2 需求内容）
> Document Type: Product Requirements
> Target: CSM V2
> 创建日期：2026-09-24

本文件是 CSM V2 产品需求的权威来源（Source of Truth）。V2 的需求内容尚未提供，本文件当前仅登记已确认的规划前提，**不得据此推断任何业务规则**。

## 已确认的规划前提（2026-09-24，用户裁定）

1. **独立建设**：V2 的需求、领域模型、架构与实现独立定义，不依赖、不参考 V1；执行边界见 `AGENTS.md` §1.1。
2. **需求文档独立**：本文件维护 V2 需求，领域模型与产品范围等待用户提供并确认。
3. **明确排除项**：集群概览聚合、搜索结果上下文展示不纳入当前范围；后续调整须由用户明确确认。
4. **无需迁移旧数据**：V2 不要求迁移已部署 V1 的生产数据。此项不免除 V2 自身后续数据库变更的数据安全与 Migration 要求。

---

## 待填写（用户提供）

> 以下为 V2 产品需求的正文，**等待用户输入后填写**。在填写并经用户确认前，不得创建 V2 Feature 或生成 V2 项目计划。

- 资源领域模型（资源类型、分类、关系、状态、唯一性、生命周期）
- 各资源能力的产品规则与验收标准
- 查询、展示与导入需求
- 认证 / 权限 / 删除 / 部署等系统级约束
- 明确排除项
