# CSM V2 产品需求

> Status: **DRAFT — DECISIONS RESOLVED (2026-09-24), AWAITING FINAL CONFIRMATION**
> Document Type: Product Requirements
> Target: CSM V2
> 创建日期：2026-09-24
> 来源：用户 2026-09-24 提供的《集群资源管理平台需求分析》开发讨论稿

本文件是 CSM V2 产品需求的权威来源（Source of Truth）。

状态标记：`CONFIRMED`（用户正文明确陈述、内部无冲突，**仍待用户最终签核**）/ `PROPOSED`（正文暂定项或需用户确认的默认值）/ `OPEN`（正文缺失或冲突，必须由用户裁定，不得由 PM / 架构 / 开发代填）。

> ⚠ 本文件当前为讨论稿整理，尚未经用户最终签核。全部 `CONFIRMED` 项在用户书面确认前不得作为最终权威；`OPEN` 项不得被推断补全。原阻塞问题已于 2026-09-24 由用户裁定（见 §11）；`Q3`、`Q5` 等非阻塞项待相应阶段确认。

---

## 0. 已确认的规划前提（2026-09-24，用户裁定，予以保留）

1. **独立建设**：V2 的需求、领域模型、架构与实现独立定义，不依赖、不参考 V1；执行边界见 `AGENTS.md` §1.1。
2. **需求文档独立**：本文件维护 V2 需求，领域模型与产品范围以本文件及 `docs/product/domain-model.md` 为准。
3. **无需迁移旧数据**：V2 不要求迁移已部署 V1 的生产数据。此项不免除 V2 自身后续数据库变更的数据安全与 Migration 要求。
4. **原有排除项变更（2026-09-24 用户裁定，BQ-D）**：原“集群概览聚合 / 搜索结果上下文展示”不纳入范围的排除项 **已被覆盖作废**；§6.1 集群入口聚合计数与 §6.2 搜索作用域展示纳入 P0。

---

## 1. 背景与目标

### 1.1 背景与用户问题 `CONFIRMED`

目前集群、服务器、网卡、IP、网段、虚拟机和服务信息分散在表格中。运维人员需要快速回答：

- 某集群有哪些主机及状态；
- 某个 IP 在哪台机器的哪张网卡上；
- 某台宿主机运行哪些 VM；
- 某项服务部署在哪些节点；
- 新增主机时如何一次完成主机、网卡和 IP 的登记。

定位：内网运维资源查询与维护平台，逐步替代分散的 Excel 台账。

### 1.2 P0 目标 `CONFIRMED`

- 按集群查看和维护裸金属、网段、VM、服务及部署实例；
- 支持主机及其网卡/IP 一次录入；
- IPv4 地址支持手动或自动分配；
- 支持从 IP、VM、服务反查实际归属；
- 保证同集群内有效计算资源名称和 IP 唯一；
- 平台记录运维台账；
- 资源状态由维护人员更新，并展示更新时间。

### 1.3 资源抽象 `CONFIRMED`（专有属性必填范围 `OPEN`）

平台统一使用“资源”作为业务概念。

P0 中，裸金属与 VM 共用计算资源身份 `Resource`，通过 `resource_type` 区分：

- `bare_metal`：裸金属；
- `virtual_machine`：虚拟机。

`cn001`、`vm001` 等均是具体资源实例，每个实例具有稳定资源 ID。

计算资源共用：所属集群；名称；资源类型；创建/更新时间；逻辑删除状态。

CPU、SN、宿主机、vCPU 等专有属性由对应类型详情维护。

网段、IP、服务在业务上也属于资源，但 P0 继续采用独立模型，不作为计算资源公共表的类型。

- `OPEN`：CPU/内存/GPU/SN/vCPU 等专有属性的必填范围（见 §11 Q3）。

---

## 2. 使用角色与典型任务

### 2.1 角色与首期权限建议 `CONFIRMED`（认证源 `OPEN`）

| 角色 | 任务 | 首期权限建议 |
| --- | --- | --- |
| 运维查看者 | 选择集群、搜索主机/IP/VM/服务、查看详情 | 只读 |
| 资源维护者 | 新增和修改主机、网段、网卡、IP、VM、服务与部署，停用或逻辑删除 | 读写 |
| 平台管理员 | 维护集群信息、账户权限及恢复/冲突处理 | 管理 |

- `OPEN`：“接入组织现有认证方式”的具体认证源（架构/部署阶段裁定，见 §11 BQ-F）。

### 2.2 典型任务路径 `CONFIRMED`

- `选择 N96P → 搜索 gpu0756 → 查看基本信息/接口/IP/VM/服务`
- `搜索 192.168.1.1 → 选择所在集群 → 定位主机和 eth0`
- `新增 cn001 → 填写 eth0、ib0 及其 IP → 一次提交`
- `搜索 NFS → 查看各部署节点及角色`

---

## 3. P0 交付范围

### 3.1 P0 交付内容与边界 `CONFIRMED`

| 模块 | P0 交付内容 | 关键边界 |
| --- | --- | --- |
| 集群和计算资源 | 统一资源列表/详情；按裸金属、虚拟机筛选；公共信息、类型详情、网卡/IP 一次录入与编辑；冲突校验、逻辑删除 | 裸金属与 VM 共用资源 ID，专有属性分别维护 |
| 网段与 IPv4 分配 | 一个集群支持多个网段；维护 CIDR、用途、VLAN、网关、自动分配范围和保留地址；支持手动或自动分配至裸金属/VM 网卡 | 不自动修改服务器网络配置，不提供 DHCP 服务 |
| 类型详情 | 裸金属维护 SN、CPU、内存、GPU 等；VM 必须关联宿主机，并维护 vCPU、内存等；支持由宿主机反查 VM | 不执行实际虚拟机创建、迁移或启停 |
| 服务 | 登记服务及其关联集群、访问入口；登记部署实例及运行节点、角色和监听信息；支持双向查询 | 不执行服务部署或健康检查 |
| 通用 | 列表与详情、条件检索、单条录入与维护、基础权限、操作审计和逻辑删除 | Excel/CSV 导入、批量更新和导出不在 P0 |

### 3.2 P0 不包含 `CONFIRMED`（仅 P0 范围，不构成永久排除）

- 自动发现；
- 故障告警；
- Excel/CSV 导入；
- Excel/CSV 导出；
- 批量更新；
- 完整 CMDB 生命周期；
- DHCP；
- 自动修改服务器网络配置；
- VM 自动创建、迁移、启停；
- 服务自动部署与健康检查。

---

## 4. 核心业务规则

### 4.1 集群与计算资源 `CONFIRMED`

1. 集群使用稳定编号 `code`，例如 `N96P`。
2. 同集群内有效计算资源 `name` 在裸金属与 VM 之间统一唯一，跨集群允许重复。
3. 裸金属原 hostname 统一使用资源 `name` 表达，表单可显示“资源名称（主机名）”。
4. 网卡和服务部署使用稳定 `resource_id` 关联，资源改名不改变归属。
5. `resource_type` 创建后不允许直接修改。
6. Cluster 是独立受管实体，属性集为稳定编号 `code`、名称、用途；由平台管理员维护。

### 4.2 网卡与 IP `CONFIRMED`

1. 同一集群内有效 IP 地址全局唯一，跨集群允许重复。
2. 同一主机可有多张网卡。
3. 一张网卡可有零个或多个 IP。
4. 无 IP 的 IB 接口允许登记。
5. 同一有效计算资源下接口名唯一。
6. 网卡录入时直接选择本集群的网络范围（网段），不再单独填写技术类型和用途。
7. 网络技术、用途、前缀、VLAN 和网关由所选网段关联展示。
8. 仅支持 IPv4。
9. **管理 IP（2026-09-24 裁定，BQ-B）**：从该计算资源已登记的网卡 IP 中指定一个作为管理 IP，不单独自由填写。资源可无管理 IP（`nullable`）；被指定的 IP 必须属于该资源。管理 IP 为资源上的引用/展示字段，不新增独立 IP 记录。
10. **“有效”定义（2026-09-24 裁定，BQ-C）**：指未被逻辑删除；系统不再有“停用”状态。

### 4.3 计算资源状态 `CONFIRMED`（2026-09-24 修订，BQ-A）

裸金属与 VM 共用同一状态模型：

- `IDLE`：空闲；
- `ALLOC`：已分配；
- `DOWN`：宕机 / 不可用；
- `UNKNOWN`：未知。

默认状态为 `ALLOC`。状态由维护人员更新，页面展示状态来源及更新时间。

> 本次修订取代原“`AVAILABLE` / `IN_USE` / `FAULT` / `UNKNOWN`，默认 `UNKNOWN`”定义。

- `OPEN`：“状态来源”的具体语义（操作者还是来源系统）。

### 4.4 删除与恢复 `CONFIRMED`（2026-09-24 修订，BQ-C）

1. 删除采用逻辑删除。
2. **系统不设“停用”状态（2026-09-24 用户裁定，BQ-C）**；资源只区分“有效（未删除）”与“已逻辑删除”。
3. 删除主机时，其网卡/IP 不得继续占用有效资源唯一性。
4. 如存在 VM 或服务部署关联，应先处理关联关系，再允许删除。
5. 恢复时重新检查资源名称、IP 和关联约束。
6. 普通列表只显示有效资源，管理员可追溯历史记录。

### 4.5 一次提交 `CONFIRMED`

一个表单提交：

- 资源公共信息；
- 与 `resource_type` 对应的类型详情；
- 全部网卡；
- 全部 IP。

整个保存过程必须作为一次完整操作：

- 任一校验或写入失败，整单失败；
- 不能出现“主机已创建但第二张网卡失败”等半完成状态；
- 编辑时必须区分“删除一张网卡”“删除一个 IP”和“未修改该项”。

### 4.6 网段 `CONFIRMED`（重叠处置 `PROPOSED`）

1. 一个集群可以有多个 IPv4 网段。
2. 每个网段只属于一个集群。
3. 同集群内有效网段名称和规范化 CIDR 分别唯一。
4. 不同集群可以登记相同 CIDR。
5. 同集群内网段重叠时提示风险，当前不禁止。（`PROPOSED`，待确认，见 §11 Q5）
6. 新增或重新分配 IP 时，地址必须属于网卡所选择的同集群有效网段。
7. P0 默认一条网卡/逻辑接口选择一个网段。
8. 该接口可分配同网段多个 IP。
9. 无 IP 网卡可仅关联网段。
10. 尚未确定网络时允许暂不选网段；分配 IP 前必须补选。

### 4.7 VM `CONFIRMED`

1. VM 必须属于一个集群。
2. VM 必须关联一台已登记宿主机。
3. 宿主机只能是同集群有效裸金属资源。
4. 宿主机不能是 VM 或自身。
5. VM 名称参与计算资源统一唯一校验。
6. VM 可有多张网卡及多个 IP。
7. VM IP 同样参与同集群 IP 唯一校验。
8. 宿主机支持通过主机名称或已登记 IPv4 地址模糊查找。

### 4.8 服务 `CONFIRMED`（访问入口基数 `OPEN`）

1. 服务是可跨集群关联的独立资源。
2. 服务编号全局唯一。
3. 一项服务可关联多个集群和多个部署实例。
4. 每个实例必须指向一条有效计算资源（裸金属或 VM）。
5. 实例所属集群必须在服务的关联集群内。
6. 服务访问入口与实例监听地址/端口分别记录。
7. **访问入口按关联集群分别配置（2026-09-24 裁定，Q4）**：每个“服务 × 关联集群”各自维护自己的访问入口（VIP / 域名 / 端口）；入口归属服务-集群关联，而非服务全局。

---

## 5. IPv4 分配规则 `CONFIRMED`

| 项目 | 规则 |
| --- | --- |
| 手动分配 | 先选择集群及网段，再填写 IPv4；允许使用自动分配范围以外、但仍在网段内的可用地址 |
| 自动分配 | 在网段配置的起止范围内按地址数值从小到大选择可用地址 |
| 自动分配范围 | 每个网段可独立启用；两端均需在网段内且起始不大于结束 |
| 不可分配地址 | 排除同集群已分配地址、管理员保留地址、已登记网关，以及普通网段的网络地址和广播地址 |
| 特殊前缀 | `/31` 仅用于明确标记的点到点网段；`/32` 按单主机地址处理 |
| 保留地址 | 支持单个地址或起止范围；保留地址不绑定网卡 |
| 地址耗尽 | 明确提示当前网段无可用地址，不自动切换其他网段 |
| 释放与恢复 | 删除 IP 或所属网卡/资源时释放占用；恢复前重新检查地址是否已被占用或保留 |

- 自动分配以平台记录为依据。
- 前端预览候选地址不构成占用，最终提交后返回实际分配结果。
- 缩小自动分配范围不会回收已有分配。
- 网段被有效网卡引用时禁止直接删除。
- 存在已分配 IP 时禁止修改 CIDR。
- 网段无“停用”状态（2026-09-24 裁定，BQ-C）；停止使用通过逻辑删除实现，已逻辑删除的网段不得再分配 IP。
- `REQUIRED`（依据 §9.3）：并发下地址不重复须由数据库/后端最终保证（技术实现交架构）。

---

## 6. 前端信息架构

> 2026-09-24 裁定：§6.1 / §6.2 集群聚合与搜索作用域展示纳入 P0（BQ-D）；`管理 IP` 定义见 §4.2.9（BQ-B）。

### 6.1 集群入口 `CONFIRMED`（聚合口径 `OPEN`）

展示：

- 集群编号；
- 名称；
- 用途；
- 有效主机数量；
- VM 数量；
- 网段数量；
- 服务数量。

支持选择集群，并记住本次选择。

### 6.2 计算资源列表 `CONFIRMED`

展示：

- 资源名称；
- 集群；
- 资源类型；
- 类型对应状态；
- CPU/vCPU/内存摘要；
- 管理 IP；
- 更新时间。

支持：

- 全部 / 裸金属 / 虚拟机切换；
- 名称/IP 搜索；
- 类型筛选；
- 状态筛选；
- 分页；
- 新增。

要求：

- 首屏必须选择或确认集群；
- 搜索 IP 时显示当前集群作用域；
- 默认按资源名称排序；
- 状态必须包含文字；
- 大约千台节点的集群必须服务端分页和筛选。

### 6.3 资源详情 `CONFIRMED`

公共展示：

- 资源名称；
- 集群；
- 类型；
- 状态；
- 状态来源；
- 更新时间；
- 网卡/IP；
- 服务。

裸金属详情增加：

- CPU；
- 内存；
- GPU；
- SN；
- 宿主 VM；
- 其他硬件信息。

VM 详情增加：

- vCPU；
- 内存；
- 宿主机。

### 6.4 新增/编辑计算资源 `CONFIRMED`

表单包含：

- 集群；
- 资源名称；
- 资源类型；
- 类型专有表单；
- 网卡卡片；
- 网络范围；
- IP 分配。

新增时根据类型动态切换字段。已有资源类型只读。一次保存公共信息、类型详情、网卡和 IP。

### 6.5 网段页面 `CONFIRMED`

展示：

- 所属集群；
- 名称；
- IPv4 CIDR；
- 用途；
- 技术类型；
- VLAN；
- 网关；
- 状态（有效 / 已删除）；
- 分配范围；
- 保留地址；
- 已分配数量；
- 可自动分配数量。

支持查看已分配 IP 及归属。

### 6.6 服务页面 `CONFIRMED`

展示：

- 服务编号；
- 服务名称；
- 类型；
- 关联集群；
- 访问入口；
- 部署节点；
- 节点角色；
- 监听信息。

支持按集群/名称筛选，并跳转裸金属或 VM。

---

## 7. 表单与交互要求 `CONFIRMED`

### 7.1 网卡/IP 录入顺序

建议顺序：`集群 → 网卡接口名 → 网络范围 → 手动/自动分配 IP`。

网络范围下拉显示：`网段名称 · CIDR · 用途`。只列出当前集群可用网段。网络技术和用途作为只读信息带出。切换集群时，清除不再匹配的网段选择。

### 7.2 校验

前端负责基础校验：必填项、IPv4/CIDR 语法、IPv4 前缀、同表单重复接口名、同表单重复 IP。

服务端负责最终：唯一性、归属关系、权限、并发冲突。

### 7.3 错误提示

错误必须定位到具体字段和记录。例如：`第 2 张网卡 ib0：10.0.0.1 已被本集群 cn002/ib0 使用`。

错误后保留用户已填写内容。提交期间禁用重复点击。成功后跳转详情并展示保存结果。

### 7.4 网段切换

切换尚未保存的网卡网段时：清除原网段的待分配地址；提示重新选择分配方式。

已有 IP 的网卡不能只改网段后保留旧地址。必须显式释放旧 IP 并在新网段重新分配。

### 7.5 VM 宿主机选择

宿主机选择为必填搜索下拉框。可输入：完整或部分主机名称；任一已登记网卡 IPv4。

候选显示：`主机名称 · 管理 IP（如有）· 集群`，并标出命中的 IP。相同资源只展示一次。多个候选必须由用户选择，不能根据模糊输入自动绑定。

---

## 8. 搜索与下拉规范 `CONFIRMED`

所有搜索框及所有下拉选择框均支持用户直接输入关键词进行模糊匹配，包括：单选；多选；关联对象选择器；固定枚举选择器。

### 8.1 支持匹配的内容

| 控件/对象 | 可输入匹配内容 |
| --- | --- |
| 集群 | 集群名称、编号 |
| 计算资源、宿主机、服务实例目标 | 资源名称、资源 ID、已分配 IPv4 |
| 网络范围 | 网段名称、CIDR、网络用途、技术类型 |
| 服务 | 服务名称、编号、类型 |
| 固定枚举 | 中文展示名、英文代码 |
| 页面搜索 | 当前页面对象名称、编号等；资源页面包含 IPv4 |

### 8.2 匹配规则

- 英文不区分大小写；
- 去除首尾空格；
- 完全匹配优先；
- 其次前缀匹配；
- 再其次包含匹配；
- 同级按名称及 ID 稳定排序；
- IPv4 搜索允许部分字符串；
- `%`、`_` 等按普通字符处理；
- P0 不要求拼音、错字纠正或语义搜索。

### 8.3 交互

- 输入后约 300ms 防抖自动更新；
- 回车可立即触发搜索；
- 搜索期间显示加载状态；
- 旧请求不能覆盖较新请求；
- 支持键盘上下选择、回车确认、Esc 关闭和清空；
- 无结果显示“没有匹配项”；
- 清空关键词后回到当前筛选条件下的初始候选。

大量资源采用服务端模糊查询与分页加载，不能只匹配当前页。

---

## 9. 非功能要求

### 9.1 安全 `CONFIRMED`

- 仅内网访问；
- 接入组织现有认证方式；
- 按查看者/维护者/管理员授权；
- 关键权限验证必须在服务端完成；
- 记录新增、编辑、删除的操作者、时间及关键变更；
- 审计日志不随资源逻辑删除而消失。

### 9.2 性能 `CONFIRMED`（目标值 `OPEN`）

以约 1,000 台裸金属/集群为初期基线：

- 主机、VM、服务、网段列表均服务端分页；
- 常用查询字段建立适当查询能力；
- 正式延迟目标在部署环境测量后确定。

### 9.3 可靠性 `CONFIRMED`（备份/保留周期 `OPEN`）

- 数据库/后端必须承担最终并发保护；
- 一次资源提交失败不能留下半条数据；
- 每日备份、恢复演练和审计保留周期由部署方案确定。

### 9.4 可用性 `CONFIRMED`

- 错误指出具体字段和冲突对象；
- 支持键盘操作；
- 状态含文字，不能只靠颜色；
- 时间显示时区；
- 窄屏表格可横向滚动。

---

## 10. P0 验收场景 `CONFIRMED`（原文保留）

1. 一次录入 `cn001 + eth0 + 192.168.1.1/24 + ib0 + 10.0.0.1/24`，详情完整可查。
2. 同集群已有裸金属 `cn001` 时，再创建同名裸金属或 VM 均被拒绝。
3. 同集群重复 IPv4 被拒绝，不同集群允许同资源名称、同 IP。
4. 一个请求中第二个 IP 冲突时，整台新主机及之前的网卡/IP 都不应残留。
5. 修改资源 name 后，网卡、IP 和服务部署仍归属原资源。
6. 删除/恢复主机时唯一性与关联可见性正确。
7. 约千台主机按资源名称、类型、状态和 IP 查找，分页与跳转详情正常。
8. 无 IP 网卡正常显示。
9. 输入 IPv6、非法 IPv4/CIDR、IPv4 前缀超 32、同表单重复接口/IP 时能定位错误且不清空表单。
10. 同一集群可以登记管理、计算和存储等多个 IPv4 网段。
11. 越界 IP、错误 CIDR 或跨集群网段关联被拒绝。
12. 重叠网段给出明确风险提示。
13. 一次录入 `vm001` 及其网卡/IP，可在 VM 详情及宿主机详情双向查看。
14. 跨集群宿主机关联与 VM/IP 冲突被拒绝。
15. 缺少宿主机的 VM 不允许保存。
16. 服务可跨集群关联，并在裸金属/VM 上登记部署实例。
17. 未关联集群的节点不能作为服务实例目标。
18. 对存在 VM 或服务实例关联的主机执行删除时，明确提示需先处理的关联对象。
19. 手动分配自动范围之外但仍在所选网段内的可用 IPv4 可以成功。
20. 越界、已占用、保留、网关及普通网段网络/广播地址被拒绝。
21. 自动分配范围 `192.168.1.20–192.168.1.30` 中，`.20` 已分配、`.21` 已保留时，下一次自动分配得到 `.22`。
22. 两个并发请求不能拿到同一个地址。
23. 地址耗尽时整单失败且不残留占用。
24. 释放地址后允许重新分配。
25. 未启用自动分配的网段只能手动分配。
26. 已逻辑删除的网段禁止新分配（原“停用网段禁止新分配”随 BQ-C 取消“停用”而改写）。
27. 新增保留范围或修改网关不能与现有分配冲突。
28. 新增网卡选择网段后，技术、用途、前缀及网关自动展示。
29. 网卡请求不需要单独录入 technology/purpose。
30. 未分配 IP 的网卡可仅选择网段保存。
31. 未选网段时拒绝分配 IP。
32. 统一资源列表可切换全部/裸金属/虚拟机。
33. 详情及新增表单按资源类型显示字段。
34. 资源类型创建后不能通过普通编辑修改。
35. 输入主机名称片段或 IPv4 片段可以找到宿主机。
36. 同一宿主机多个 IP 命中时按资源去重。
37. 每个搜索框、单选/多选下拉均可直接输入。
38. 模糊匹配覆盖全部符合条件的数据，而非仅当前页。
39. 快速连续输入不会出现旧结果覆盖。
40. 集群切换及权限限制生效。
41. 关联字段最终提交稳定资源 ID，未匹配输入不能意外创建资源。

---

## 11. 决策记录与开放问题

### 11.1 已由用户裁定（2026-09-24）

| 编号 | 问题 | 裁定 |
| --- | --- | --- |
| BQ-D | 集群聚合 / 搜索作用域展示与旧排除项冲突 | 覆盖：纳入 P0（§0.4、§6.1、§6.2） |
| BQ-A | VM 状态模型 | 与裸金属共用 `IDLE` / `ALLOC` / `DOWN` / `UNKNOWN`，默认 `ALLOC`（§4.3） |
| BQ-B | `管理 IP` 定义与来源 | 从资源已登记网卡 IP 中指定一个，可空（§4.2.9） |
| BQ-C | “停用”与“有效” | 取消“停用”概念（含网段）；“有效”= 未逻辑删除（§4.2.10、§4.4、§5、§6.5） |
| Q4 | 服务访问入口是否按集群配置 | 按“服务 × 关联集群”分别配置（§4.8.7） |
| Q1 | 状态由谁维护 | 维护人员更新；状态来源语义仍待定 |
| Q2 | 删除 / 恢复关联处理 | 见 §4.4 |
| Q5 | 网段重叠处置 | 暂定仅提示风险，待最终确认（`PROPOSED`） |

### 11.2 仍开放（Non-blocking，登记后续阶段）

- Q3：CPU/内存/GPU/SN/vCPU 等专有属性的必填范围（Feature / 架构阶段前裁定）。
- Q5：网段重叠仅提示还是禁止写入（暂定仅提示；确认后可能影响写校验）。
- “状态来源”具体语义（操作者还是来源系统）。
- BQ-E：“节点角色”“服务类型”是否为受控枚举及其取值集。
- BQ-F：认证源、审计保留周期、备份与恢复演练、正式延迟目标（部署阶段）。

### 11.3 讨论稿原“当前暂定”项

- 网段重叠只提示风险（`PROPOSED`）；
- 仅支持 IPv4（`CONFIRMED`）；
- 一个集群支持多个网段（`CONFIRMED`）；
- 手动/自动分配均属于 P0（`CONFIRMED`）；
- 保留地址管理属于 P0（`CONFIRMED`）；
- VM 和服务均属于 P0（`CONFIRMED`）。

---

## 附录 A：建议的产品能力域（Epic 候选，最终由项目规划确定）

| Epic 候选 | 能力边界（用户可观察） | 验收映射 |
| --- | --- | --- |
| E1 集群管理 | 集群登记与选择；作为其它资源归属；列表记住本次选择。聚合计数是否纳入待 BQ-D 裁定。 | 1(部分)、40 |
| E2 计算资源统一身份与登记 | 统一资源列表/详情、类型切换、筛选、分页、按名/IP 搜索；一次提交公共信息+类型详情+全部网卡+IP；整单原子；逻辑删除/恢复；改名不改归属；类型创建后只读。 | 1–8、32–34 |
| E3 类型详情与宿主关系 | 裸金属详情与状态维护；VM 必须关联同集群有效裸金属宿主；宿主反查 VM；VM/IP 唯一与跨集群校验；宿主模糊查找与去重。 | 13–15、35–36 |
| E4 网段与 IPv4 分配 | 集群多网段；CIDR/用途/技术/VLAN/网关/自动范围/保留地址维护；手动与自动分配；唯一性与可用性校验；并发不重复；耗尽整单失败；释放重用；停用禁新分配；CIDR 引用保护；重叠风险提示；网段带出只读属性。 | 1、3–4、9–12、19–31 |
| E5 服务与部署实例 | 服务编号唯一、跨集群关联；实例指向有效计算资源且集群受限；访问入口与监听分离；双向查询与跳转。 | 16–18 |
| E6 通用检索与下拉 | 所有搜索框/单选/多选/枚举可输入模糊匹配；完全>前缀>包含；稳定排序；服务端模糊+分页；防抖、竞态防护、键盘交互、无结果提示。 | 37–39、41 |
| E7 权限、审计与非功能基线 | 三角色服务端鉴权；操作审计不随逻辑删除消失；每单失败无半条数据；状态含文字、时区、键盘可用、横向滚动；~1000 基线分页。 | 40，及 §9 |

---

## 附录 B：确认记录

| 日期 | 事项 | 结果 |
| --- | --- | --- |
| 2026-09-24 | 用户裁定独立建设、需求文档独立、无需迁移旧数据、原有排除项 | 见 §0 |
| 2026-09-24 | 用户提供开发讨论稿正文 | 本文件整理，待最终签核 |
| 2026-09-24 | 用户裁定 BQ-D / BQ-A / BQ-B / BQ-C / Q4 | 见 §11.1，已写入正文 |