# CSM V2 计划 revision 7 变更盘点（阻塞，非审批稿）

> Status: **PROPOSED — BLOCKED — NOT READY FOR APPROVAL**
> 本文件仅盘点从已批准 revision 6 到最新 BQ-I–BQ-O 产品裁定的影响，**不是项目机器状态或已批准计划**。唯一机器状态来源仍是 `docs/project/project-plan.yaml`（revision 6 `ACCEPTED`，11 个 Feature 均 `BLOCKED`，无执行检查点）。
> BQ-M 将全部受管资源改为真实删除，覆盖了此前本提案“仅集群真实删除”的假设；BQ-N/BQ-O 又确认逐项先删、管理员可查历史及网段删除前后地址/保留/网关处理。因此旧 revision 7 草案作废，不得按旧稿批准、调度、实施或将当前 62 条场景视为完整覆盖。待需求与验收缺口澄清后，在此编号下重新规划并单独送用户批准；不得静默修改 revision 6 基线。

## 1. 权威输入及裁定边界

- `docs/product/requirements-v2.md` §11.1 BQ-I–BQ-O 与 §11.2，`docs/product/domain-model.md`：产品事实的权威来源；本提案不重复维护详细规则。
- BQ-M 最新确认：**全部受管资源**取消逻辑删除与业务恢复，统一真实删除；仍存业务关联须先显式删除/解除，已解除仅留历史的关联不阻止；审计与资源历史保留。集群真实删除允许资源维护者和平台管理员，查看者不得操作；真实删除后集群 `code` 不复用、名称可复用。集群名称仅允许字母/中文/下划线/数字，含空格的输入直接拒绝，判重区分大小写。
- BQ-N 补充确认：上级删除前仍存子项/关联逐项先删；网段全部已分配地址须先清空；资源历史至少记录操作人与删除/变更内容，管理员可查询。已确认的审计记录仍包括操作者、时间及关键变更。
- BQ-O 补充确认：删网段前逐条删除保留地址、显式清空网关属性；删后历史保留/网关不阻止其它仍存重叠网段分配；仍存网段间的跨重叠排除不变。重叠网段可自动分配数量的**展示口径**仍 `OPEN`。
- BQ-I/BQ-J/BQ-K/BQ-L 的冲突部分由 BQ-M 覆盖；之前对当时确认范围的有条件签核**不等于**本次新范围已完整签核或获实施授权。
- 已批准执行基线：`docs/project/project-plan.yaml` revision 6，批准提交 `350870fcb6ddf79bd9a57f43c6b14fc2e5a903b0`；revision 6 包含 11 个 P0 Feature、57 条已映射场景、全部 `BLOCKED`、`execution.current_feature/current_stage: null`、没有交付 Git/Test/Review 证据。该历史基线不因本提案或产品文档落稿被篡改。

## 2. 已批准范围的变更影响（待正式重规划）

| 范围 | revision 6 前提 | 最新需求与重规划工作 |
| --- | --- | --- |
| 集群身份 F001 | `code` 全局唯一、名称/用途可维护、关联删除保护 | 补集群名称字符校验与区分大小写、`code` 删除后永不复用及历史承载、真实删除权限与关联前置、名称删除后可复用；F001 验收/层次需重新核定。 |
| 计算资源 F002/F004 | 逻辑删除/恢复、恢复网卡/IP、宿主及部署保护 | 删除/恢复阶段需重写；所有业务对象真实删除，网卡/IP 等仍存子项须逐项先删、不级联；必需关系的合法显式解除方式与历史载体仍待确认，可能影响 Feature 边界和依赖。 |
| 网段/IP F005/F006 | 逻辑删除网段、IP 释放与恢复、历史保留/网关 | 改为真实删除；删前须逐项删除全部已分配地址/保留地址、显式清空网关并处理网卡引用；删后历史保留/网关不再排除其它仍存重叠网段分配，计数展示仍待决。 |
| 服务 F007 | 服务及服务—集群关联的删除保护、入口恢复处理 | 真实删除、部署实例及访问入口的显式清理与历史留存待确认，不得隐式级联。 |
| 权限/审计 F009 | 全平台三角色，审计不随逻辑删除消失 | 保留审计与资源历史（资源历史至少操作人+删除/变更内容，管理员可查；审计原有时间要求不撤销）；集群真实删除允许非只读用户，服务端鉴权；其它历史查询权限/保留期限仍 OPEN。 |
| 其他 Feature / Milestone | 依赖和验收按 revision 6 的软删/恢复规则安排 | 受影响的搜索、列表、聚合、状态、M1–M4 验收和依赖需逐项重新核对；不能预先断定 11 个 ID、依赖边、里程碑分布或覆盖归属完全不变。 |

以上为**影响候选**，不是已批准的 Feature scope/acceptance。审计/资源历史必须与已删除的在用业务对象区分；`code` 永不复用的持久完整性由架构/数据库方案保证，不预设直接删行或清除历史。

## 3. 验收覆盖缺口（阻止送审）

- 产品文档 §10 当前已有 **1–62** 条（旧 57 条已局部改写，新增场景 58–62）。revision 6 只覆盖旧 1–57；旧映射、`parts`、`closure_owner`、计划中 `gaps: []` 与 `plan_completeness: complete` **不适用于新范围**。
- 现有 1–62 已覆盖逐项先删、网段分配地址/保留地址/网关清理、删后历史保留/网关不参与分配与管理员可查最少历史内容，但尚未完整验收其它受管子项（如访问入口）的真实删除/解除、历史查询范围及重叠网段可自动分配数量的展示口径等。集群 `code` 删除后不复用已在 §10.50/51 明确，但仍须更新旧计划映射。见 `requirements-v2.md` §10 的验收缺口。
- **不可现在宣布 62 条覆盖完整、无 gaps、计划就绪或任何 Feature READY/DONE**。应先由 Product Manager 补充可验证的验收，随后 Project Manager 重新建立 Feature/场景/里程碑映射、检查独立验收价值与依赖 DAG，并同步三份派生视图；届时再提出完整 revision 7 审批稿。

## 4. 待用户确认（影响设计或验收，不能代填）

1. `DELETE-DEPENDENCIES`：上级删除前逐项先删、网段全部已分配地址与保留地址先删、网关显式清空已确认；访问入口等其它子项与必需关系如何合法解除仍待定。网关是属性，不推定其为独立资源，也不允许隐式级联。
2. `RESOURCE-HISTORY`：至少保留操作人与删除/变更内容且管理员可查已确认；其它角色能否查询、历史与审计边界、保留期限仍待定，既有审计记录时间要求不撤销。持久实现方式交架构/数据库，不因真删清除历史。
3. `SEGMENT-RETENTION` / `IP-LIFECYCLE`：删网段前逐项删全部已分配地址/保留地址、清空网关，删后历史保留/网关不再影响其它仍存重叠网段分配已确认；仍待确认的是**重叠网段可自动分配数量的展示口径**及单条网卡/IP 的历史表达与释放细节。
4. `NAME-COMPARISON` / `CODE-FORMAT`：其他资源名、接口名、网段名、服务编号的比较规则；集群名称已允许字母/中文/下划线/数字，其字符类别具体边界与长度如需限制仍待确认，`code` 自身格式另议。不得把 §8 搜索口径套用为唯一性规则。
5. `DELETE-CONFIRMATION` 与 §11.2 其余 OPEN：真实删除是否需额外确认，以及服务入口/角色、状态来源、认证/审计期限等分阶段裁定。

已确认的“全部资源真实删除”“逐项先删/网段分配地址和保留地址先删、网关先清空”“删后历史保留/网关不参与分配”“历史最少内容且管理员可查”“集群 code 永不复用/名称可复用”“集群维护者及管理员可删”等不是待重问的事项。没有完整的产品与验收输入前，不进入架构/数据库/Contract 或实施。

## 5. 状态、安全与后续

- **现状**：revision 6 `ACCEPTED` 仅作为已批准历史执行基线；产品范围现已变化，实际无 READY Feature。revision 7 为**阻塞的非审批稿**，`project-plan.yaml` 与 `backlog.md`、`dependency-map.md`、`milestones.md` 仍维持 revision 6，不能伪称派生视图已经同步新规则。
- **Git**：本提案不写入任何 Feature 的 `git`、`evidence`、Review/Test 或 DONE；无分支/实现任务已启动。用户授权提交本次产品文档与阻塞盘点，不构成批准 revision 7 或授权推送/实施。
- **下一步**：用户裁定 §4 的必要产品口径 → Product Manager 完成验收与领域更新 → Project Manager 根据最新确认范围重做 revision 7 计划及派生视图 → 用户**单独批准完整计划** → 架构/Contract/测试/Review 等门禁通过后才允许实施。不得把 BQ-M 视作对旧 revision 6 的实施放行。
